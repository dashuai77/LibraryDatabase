Library (libraryID, name, address)

Book (ISBN, title, price, pubDate, authorID)

Author (authorID, Name, Address)

Storage (libraryID, ISBN, qKeep, qRent, qSale)

// Customer and Staff can be a same class
Customer (CustomerID, pwd, name)

CustomerPhone (CustomerID, phone#)
PaymentMethod (CustomerID,Account#)

Staff (staffID, pwd, name, Salary) optional subclass: administrator ()


Rent (Order#, customerID, ISBN, date, retdate, status)
Purchase (Order#, customerID, ISBN, date)
PickUp(Order#, ISBN, pickupTime, status)
Deliver(Order#, address, deliverer(staffID), status)

Request (ReqID, CustomerID, ISBN, libraryID)

Honesty (CustomerID, lateTime, lateDay, borrowTime, borrowDate)
